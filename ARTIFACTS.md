# Artifact Manifest and Validation

This repository defines a reproducible artifact contract in `manifest.json`.

## What is in scope
Only the main-body figures/tables currently implemented in code:
- Fig. 6–10
- Table 4–5

(Additional appendix and hardware experiments are tracked as separate checklist items and are not claimed here.)

## How to reproduce
Run all main experiments:
```bash
python -m experiments.run_all